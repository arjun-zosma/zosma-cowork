{
  "name": "zosma-oauth-broker",
  "version": "1.0.0",
  "description": "Stateless Google OAuth token broker for Zosma Cowork. Holds the Web-application client secret server-side; the desktop app sends PKCE auth codes / refresh tokens here for exchange so no secret ever ships in the client.",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-functions": "^6.1.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
